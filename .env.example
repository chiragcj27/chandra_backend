PORT=
MONGODB_URI=

# Auth
JWT_SECRET=
JWT_EXPIRES_IN=7d

# Used to derive deterministic (but not name-guessable) client passwords.
# Must be kept secret and should be unique per deployment.
PASSWORD_SEED=
ADMIN_EMAIL=
ADMIN_PASSWORD=
